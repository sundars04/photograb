.posts-wrapper.row
  - @posts.each do |post|
    .post
      .post-head
        .name
          Test User
      .image.center-block    
        = link_to (image_tag post.image.url(:medium), class:'img-responsive'), post
      %p.caption
        = post.caption